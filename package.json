{
  "name": "umbra",
  "version": "1.0.0",
  "description": "A Doom-shaped FPS in WebGPU that ships as one zipped index.html",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "test": "node test/logic.mjs",
    "pack": "node build/pack.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.12",
    "roadroller": "2.1.0"
  }
}
